# 通話シグナリング

Call アダプターは、ルーム単位の通話招待、呼び出し、応答、終了、タイムアウト、ブラウザープロバイダーへの引き渡しを所有します。Messages は汎用的なルームアクションフローを公開し、Call アダプターがそのステージへ操作と動作を注入します。

通話はスレッドヘッダーを残し、チャット履歴とメッセージ入力欄を置き換える呼び出し画面から始まります。受信者には、応答操作で通話トークン付きのルームを開く継続的な Calls 通知が届きます。未応答の通話は 45 秒後に期限切れになります。応答後、アダプターは `phase: connect` で `voip:startCall` を呼び出し、プロバイダーはコンポーネントまたはナビゲーションアクションを返します。埋め込みミーティングの操作は Call ツールバーと分離され、その矢印でミーティングをピクチャーインピクチャーへ移動して Messages を復元します。

## 使用例

Call アダプターは、Messages と `voip:startCall` ブラウザプロバイダーが利用可能な場合に自動的に有効になります。

## 技術仕様

`social:callUi` ケイパビリティは、招待状態、45 秒のタイムアウト、通知の応答リンク、コンポーネントのマウント、通話終了、プロバイダーへの引き渡しを管理します。

着信は通知ベルの一覧には追加されません。応答、拒否、または期限切れになるまで短時間通知の領域に表示され続け、緑の応答操作と赤の拒否操作を提供します。Messages を開いている間、対象ルームは一時的にルーム一覧の先頭へ移動します。発信者には、キャンセル、受信者の拒否、未応答、Meeting プロバイダーによる引き渡し拒否を区別したフィードバックが表示されます。

各通話の状態変化は Messages のルーム履歴にルームイベントとして保存されるため、すべての参加者が発信、応答、キャンセル、拒否したユーザーと未応答になった通話を確認できます。ブラウザーは、招待が応答、拒否、キャンセル、または期限切れになるまで、着信と発信で異なる合成呼び出し音を繰り返し再生します。

最新の招待が呼び出し中の間、そのルームイベントは操作可能な通話カードになります。受信者は会話内で応答または拒否でき、発信者には呼び出し状態が表示されます。通話状態が変化するか新しい通話が始まると、履歴項目は操作のない通常イベントになります。着信プロンプトはシェル起動時に保存済み通知から復元され、呼び出し音にはより強い反復パルスが使われます。

応答済みコンポーネントのピクチャーインピクチャー移動は冪等です。矢印を再度操作してもフローティングウィンドウは再作成またはリサイズされません。最初の操作で矢印とインライン領域を隠し、会話を復元して再描画し、コンポーネントが閉じるまでカメラ操作を無効化します。アクティブな通話カードには塗りつぶし背景とアニメーション枠を使い、終了済みイベントには応答・拒否操作を残しません。

通話コンポーネントをピクチャーインピクチャーへ移動すると、永続的なドキュメントシェルへ移され、呼び出し元ページの中断や SPA ルートのクリーンアップ後も維持されます。明示的にコンポーネントを閉じた場合は完全なクリーンアップを行い、元のホストが接続中なら復元します。

保持された PiP ポータルは、安定した要素 ID を含むブローカー所有のコンポーネントステージをウィンドウの親として維持します。Jitsi などのプロバイダーは、参加者の退出、キック、または会議終了時に親ステージを解決し、ホストの `component-pages:discard` ケイパビリティを呼び出せます。破棄後は一時的な通話ステージも削除されます。

アクティブなコンポーネントを PiP に移動しても、プロバイダーは安定したコンポーネントホストにマウントされたままです。境界付きホスト自体がフローティング画面となってプロバイダー内容を寸法内に収め、同じライブコンポーネントを Messages の通話画面へ戻す半透明の閉じるコントロールを任意で表示できます。

アクティブな通話では、現在参加しているアカウントを記録します。発信者と最初の応答者だけでグループ通話が開始され、ほかの招待者は後から参加できます。プロバイダーの終了時にローカルアカウントが退出し、最後の参加アカウントが退出すると通話が解放されるため、カメラ操作で新しい招待を作成してルーム内のほかの全参加者へ通知できます。

コンポーネントプロバイダーの解決は、最初のクリックが完了した後になる場合があります。そのため通話 UI は、開始または応答時にコアの一回限りのコンポーネント起動許可を同期的に取得し、後続のコンポーネントマウントへ渡します。許可は 60 秒後に失効し、2 つ目のウィンドウを認可できません。

プロバイダーはコンポーネントアクションに `context.allowNavigation: true` を設定し、PiP 画面の最小寸法を `minSize` で指定できます。通話 UI はその許可をコンポーネント起動へ渡し、PiP ホストを永続シェルへ移動しますが、ナビゲーション保持を有効にするのは通話が PiP に入った後だけです。通話を Messages に戻すと、既存のプロバイダーホストを再接続し、次のナビゲーションに備えてルート単位のクリーンアップを復元します。

着信は、認証済みのユーザー単位 `/ringing` リースを使用します。ブラウザー画面は呼び出し中にリースを更新し、解決時に解放するため、着信音を再生するタブまたはプロンプトは一つだけです。応答または拒否すると、相関した解決通知によって通知と Messages のプロンプトが同時に閉じます。

Call ホストはプロバイダーのコンテキストを保持し、起動する通話コンポーネントを明示的に `voipCall` としてマークします。これにより Jitsi Meet のコンポーネント契約に合致し、使い捨て PiP 通話ではミーティングチャットを表示しません。

フローティングウィンドウのクリーンアップは保存済みの移動先階層を検証し、状態を保持する `moveBefore` が拒否された場合は通常の DOM 移動へフォールバックします。どちらの移動も構造的に無効な場合は、未処理の拒否を発生させず、所有者が破棄できるようポータルをその場に残します。

プロバイダーコンポーネントを Messages 内にドッキングしている間、通話中のスレッドは 2 行グリッドに切り替わり、Call ステージ、コンポーネントホスト、コンポーネントウィンドウがウィジェットカードの残りの高さを満たします。PiP は引き続き独立して制限されたフローティング寸法を使用します。

通話終了後も `/ringing` リースエンドポイントは冪等です。遅れて届いた更新や解放は、通話が見つからないエラーではなく、呼び出しなしの成功結果を返します。別のページへ移動した後でピクチャーインピクチャー通話を閉じようとすると、Calls は Messages に戻る、通話を終了する、またはキャンセルする選択肢を表示します。Messages に戻る場合は SPA ナビゲーションを使用し、ミーティングを再マウントせず既存のプロバイダーホストを再接続してからピクチャーインピクチャーを閉じます。

ピクチャーインピクチャーの終了ハンドラーは、終了選択肢を表示または実行する前にステージのライフサイクルからアクティブな通話を取得し、ナビゲーション後の古いスコープによるエラーを防ぎます。終了コントロールは標準のフローティングウィンドウ寸法を再利用し、破壊的な結果を示す `btn-cancel` スタイルを使用します。

通話からの退出は冪等な終了処理です。サーバーがすでに通話を終了した後でミーティングプロバイダーが閉じた場合、エンドポイントは終了済みの通話を返し、UI はエラートーストを表示せずにクリーンアップを完了します。ピクチャーインピクチャー通話を Messages に戻した後のルートクリーンアップは、破棄前に通話が再びピクチャーインピクチャーへ移動したか確認するため、2 回目の移動も SPA ナビゲーション中に維持されます。
