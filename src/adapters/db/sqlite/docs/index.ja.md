# SQLiteデータベースアダプター（削除済み）

SQLiteはサポートされなくなりました。SQLiteアダプターはCognisから削除されました。

プラットフォームは現在、データベースバックエンドとして **PostgreSQL**（デフォルト）と **MariaDB** をサポートしています。`DB_TYPE` を `postgresql` または `mariadb` に設定し、`DATABASE_URL` 接続文字列を提供してください。

設定の詳細については、[データベースゲートウェイドキュメント](/docs/gateways/db)を参照してください。
