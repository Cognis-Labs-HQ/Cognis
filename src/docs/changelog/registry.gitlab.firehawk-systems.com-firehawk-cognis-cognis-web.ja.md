# 移植可能な環境設定

**機能ブランチ:** N/A

## envファイルなしで実行

コンテナオーケストレーターは、環境変数だけで設定全体を注入できるようになりました。指定された `DATABASE_URL` はそのまま使用され、`DB_TYPE` がない場合は対応URLスキームからデータベースプロバイダーを選択します。

## 配備方式に依存しないエラー

エントリポイントの検証は、Composeが生成するファイルやセットアップコマンドを必須とせず、不足しているコンテナ環境値を説明するようになりました。

## コミット
