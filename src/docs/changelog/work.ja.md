# ホワイトボード修正

## 自動ストローク色がすぐにテーマへ追従

自動ストローク色で作成されたホワイトボード要素は描画時に色を解決するようになり、アプリのテーマ変更直後にライトテーマとダークテーマの色へ切り替わり、キャンバス背景も同時に再描画されます。

## ホワイトボード名変更リクエストを安全に検証

名前変更処理は信頼できるボード ID を保持し、送信されたタイトルをトリムし、不正なリクエストには汎用的な Bad Request ではなく明確な検証エラーを返すようになりました。
