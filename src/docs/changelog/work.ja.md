# 名前空間ベースのメディア保存統合

## ホワイトボードに貼り付けた画像がファイル名前空間を使用

ホワイトボードに貼り付けた画像は `whiteboards` ファイル名前空間へアップロードされ、完全な data URL をシーンスナップショットへ直接埋め込む代わりに、ホワイトボード画像 URL として保存されます。

## クラス教材が名前空間クライアントを公開

クラスアダプターは `classes` 名前空間クライアントをバインドして提供するようになり、今後のクラス教材 API は最初の書き込みから同じ名前空間ベースのファイル保存経路を使用できます。
