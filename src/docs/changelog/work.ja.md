# 安全な性能テレメトリ

## ブラウザ送信の強化

ブラウザ性能データの送信に認証済みセッションを必須とし、アカウント単位の頻度制限とメトリクス件数制限を適用しました。SPA 遷移でも文書メトリクスを保持し、Web Vitals のセッションウィンドウで CLS を計算します。

## より安全なエッジ既定値

遅延 TLS モードのオリジンは既定でループバックにバインドし、信頼済み転送ヘッダーを維持します。セットアップは初期自己署名証明書を生成し、ハッシュ付きアセットは esbuild の完全なハッシュ文字集合に対応します。
