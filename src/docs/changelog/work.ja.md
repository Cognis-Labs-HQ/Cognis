# 接続テストを明確化

## LDAP のバインド失敗を分かりやすく表示

LDAP 設定でディレクトリエラーコード 0x31 を、バインド DN とパスワードの確認を促す案内へ変換します。詳細な原因は構造化されたサーバーログに保持されます。

## SMTP テストで配信キューを使用

SMTP テストメッセージは、アダプターが所有するキューとレート制限を通るようになりました。失敗時には一般的なリクエストエラーではなく、具体的で対処可能な応答を返します。
