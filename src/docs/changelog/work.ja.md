# 共有表示修正

## ホワイトボード共有表示を修正

ホワイトボード共有リンクは、最初の renderer フック結果ではなく一致するページ renderer を選ぶようになりました。これにより、汎用の利用不可メッセージではなく共有ホワイトボードアプリが表示されます。

## Canvas の overflow を抑制

ホワイトボード Canvas はコンテンツが収まっている間は標準サイズを保ち、要素が表示範囲を実際に超えた場合にだけ overflow 領域を追加します。
