# ホワイトボード永続化

## 更新後も内容を保持

ホワイトボード要素のスナップショットを Cognis API 経由で保存し、各セッションで返すようにしたため、有効なユーザーと共有ゲストは同じ URL で同じ内容を読み込めます。

## 共有とオーバーフロー修正

ホワイトボード共有フックをシステム Flow コンテキストに登録してリンク作成を正しく承認し、Canvas のオーバーフローサイズは座標回収後に境界を再計算します。
