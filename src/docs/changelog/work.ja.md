# 安定したフォーカス制御

## フォーカスセッションの一貫性

フォーカス領域で省略可能な初期状態を利用できるようになり、宣言済みアプリルートが正しく解決されます。ローカルで閉じても共有セッションは終了せず、遠隔で終了したセッションの古い内容も残りません。

## 読みやすい Composer コード

グリッド寸法の処理をテスト済みの専用モジュールに分離し、ページ Composer の初期化に説明コメントと適切な空行を復元しました。
