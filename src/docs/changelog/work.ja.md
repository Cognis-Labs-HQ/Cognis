# ホワイトボード同期修正

## 要素保存の同期を維持

ホワイトボードのスナップショット保存は、ボードのタイムスタンプ更新時に構造化 DB の update フィールドを使うようになり、Canvas 編集後の 400 応答を防ぎます。

## ルートテストを実動作に近づける

ホワイトボードのルートテストは構造化 update ペイロードを扱うようになり、今後の永続化回帰を本番 DB の挙動に合わせて検出します。
