# ホワイトボード更新

## 直接更新でも読み込み完了

ホワイトボードページは共通の直接マウントヘルパーを使うようになり、ホワイトボード URL を更新しても SPA ナビゲーションと同じように読み込み状態が解除されます。

## 描画プレビューを正しく表示

ペンのプレビューは分離済みの要素レンダラーを使うようになり、ホワイトボード要素の描画ヘルパーを別モジュールへ移動した後の実行時エラーを防ぎます。
