# 高速なダッシュボード起動

## ダッシュボードカードを独立して読み込み

基本レイアウトを直ちに表示し、アカウント情報、今後の予定、拡張機能を並行して反映するため、任意の連携機能が失敗しても利用可能なページは維持されます。

## 今後の予定を1回の制限付きリクエストで取得

Calendar Gateway のフローが、アクセス可能なカレンダーの予定と招待を、呼び出し側が指定した件数上限を使う認証済みエンドポイントからまとめて返します。
