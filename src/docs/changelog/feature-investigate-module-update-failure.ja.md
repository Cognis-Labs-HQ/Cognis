# 信頼できるモジュール更新

## 更新時の依存関係確認を延期

モジュール更新では、モジュールが無効な間にチェックアウトを置き換え、依存関係の準備確認を通常の有効化フローまで延期するようにしました。これにより、インストール済みモジュールの一時的なランタイム状態が原因で、同じバージョンの有効なコミット更新が HTTP 422 で失敗することを防ぎます。有効化には、引き続き宣言されたすべての依存関係が必要です。
