# Reliable Production Assets

## Public icons are included

Production builds now place public UI assets at the direct `/assets` paths used by the server, preventing missing Cognis icons and other static files.
