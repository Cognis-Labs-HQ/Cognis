# Stable page composer edit layout

## Edit mode keeps grid placement stable

Page composer cards now share the same available-width grid sizing and grid gap in view and edit mode, while edit handles no longer reduce the visible card height. Edit mode also grows with the document when extra grid rows are added, preventing a nested vertical overflow area while users move cards downward.
