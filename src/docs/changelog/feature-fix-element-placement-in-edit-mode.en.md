# Stable page composer edit layout

## Edit mode keeps grid placement stable

Page composer cards now use the same fixed grid track sizing in view mode that edit mode uses for its drag-and-resize overlay. This prevents cards from shifting horizontally when users enter or leave layout editing.
