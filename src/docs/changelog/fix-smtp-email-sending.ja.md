# SMTP配信修正

## EHLOフォールバック改善

`HOST` が未設定の場合、SMTPアダプターはEHLOホスト名をより安全に解決するようになりました。`ehloHostname`、`from` の送信元ドメイン、SMTPホストの順に使用し、最後にのみ `localhost` を使います。

これにより、`EHLO localhost` を受け付けないサーバーでのSMTP拒否を減らせます。

## SMTP回帰テスト

`HOST` 環境値がない場合に、テストメール送信が送信元ドメインのEHLOフォールバックを使うことを確認する、SMTPアダプターの重点テストを追加しました。
