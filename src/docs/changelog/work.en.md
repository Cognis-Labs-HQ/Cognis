# TFA Email Setup Fix

## Email TFA setup now shows the missing email address reason

Email two-factor setup now opens the setup flow directly for unconfigured methods, so accounts without an email address see the specific missing-address message instead of a generic availability error.
