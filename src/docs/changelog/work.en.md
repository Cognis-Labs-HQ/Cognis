# Expose profile identity tools

## Share canonical handle resolution

The Social Profile adapter now exposes canonical single- and multi-handle normalization plus account-ID-to-handle resolution through the public `social:profile:identity` capability for trusted module orchestration.
