# Visible TFA Method Selector

## Always show the selected method

The login screen now displays the two-factor authentication method selector for every configured user, including when only one method is available.
