# Consistent profile avatars

## One profile-owned avatar source

Profile images, fallback initials, and deterministic colours now come from the profile adapter through the UI CTX capability, preventing different surfaces from producing inconsistent avatars.
