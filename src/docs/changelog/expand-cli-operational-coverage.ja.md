# CLI 対応範囲

## 運用コマンド

CLI に TFA、通知、メールアドレス、招待、カレンダー、学習言語、メッセージ会話、共有のコマンドが追加され、管理者が `cognisctl` からより多くのアプリ機能を扱えるようになりました。

## 対話型ウィザード

複雑なペイロードを持つコマンドは、引数が指定されていない場合に必要な値を質問できるため、構造化された API 操作を正しく送信しやすくなりました。
