# Reliable Document Scrolling

## Full content remains reachable

Documentation and changelog pages now use natural document scrolling after client-side navigation, so long content remains fully accessible without requiring a refresh.
