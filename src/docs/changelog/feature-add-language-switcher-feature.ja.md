# すばやい言語切り替え

## 任意の切り替えボタン

言語切り替えは既定で表示され、横並びの言語表を含む一続きの設定ブロック上部にある「言語切り替えを表示」で変更できます。ダッシュボードが保存済みの言語優先順位を直接読み取るため、切り替えボタンが確実に表示されます。クリックするたびに優先言語を順番に選択し、追加のクリックがないまま5秒経過すると、その言語を優先順位の先頭に移動してページを再読み込みします。
