# すばやい言語切り替え

## 任意の切り替えボタン

言語切り替えは既定で有効になっており、横並びの言語表を含む一続きの設定ブロック上部にある「言語切り替えを表示」で変更できます。ダッシュボードは保存済みの言語優先順位を読み取って正規化し、切り替え可能な言語が2つ以上ある場合にだけボタンを表示します。クリックするたびに優先言語を順番に選択し、追加のクリックがないまま5秒経過すると、その言語を優先順位の先頭に移動してページを再読み込みします。
