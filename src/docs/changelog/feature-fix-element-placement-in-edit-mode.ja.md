# page composer の編集レイアウト安定化

## 編集モードが通常モードの寸法を使用

page composer の編集オーバーレイは、通常モードと同じコンテンツセクション寸法から列を測定し、行の高さは通常モードの行サイズに固定するようになりました。画像、動画、音声、canvas コンテンツ、明示的に opt-in した埋め込みは、composer の再レンダー時に再作成されるのではなく一時退避して再接続されます。一方、API 管理のミーティング iframe は対象外となり、パスワード処理と復旧ラッパーが制御を維持します。
