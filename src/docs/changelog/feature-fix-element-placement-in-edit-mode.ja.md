# page composer の編集レイアウト安定化

## 編集モードでもグリッド配置を維持

page composer のカードは、表示モードと編集モードで利用可能幅に合わせた同じグリッドサイズとグリッド間隔を共有し、編集ハンドルが表示上のカード高さを縮めないようになりました。編集モードでは、カードを下へ移動するためにグリッド行が増えた場合もドキュメントとともに自然に伸び、個別の縦スクロール領域が発生しません。
