# page composer の編集レイアウト安定化

## 編集モードが通常モードの寸法を使用

page composer の編集オーバーレイは、通常モードと同じコンテンツセクション寸法から列を測定し、行の高さは通常モードの行サイズに固定するようになりました。画像、埋め込み、canvas コンテンツ、ミーティング iframe などのメディア要素は、composer の再レンダー時に再作成されるのではなく一時退避して再接続されるため、編集切り替え、移動、ポップアップ、通知による中断的な再読み込みを防ぎます。
