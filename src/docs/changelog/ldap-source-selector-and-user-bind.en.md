# LDAP Login Verification

## Responsive source selection

Named LDAP sources retain their position above the username field and adapt to the available width. Additional sources remain accessible from a scrollable overflow selector.

## Verify a real user

LDAP setup now ends with a required user credential test that previews the directory identity, groups, and resulting Cognis role before the server can be saved.
