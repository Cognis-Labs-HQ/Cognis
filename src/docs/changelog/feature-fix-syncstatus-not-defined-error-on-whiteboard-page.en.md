# Whiteboard Reliability

## Whiteboard pages open again

The whiteboard now reads synchronization state from its status controller, preventing a missing variable from stopping the page during mounting.
