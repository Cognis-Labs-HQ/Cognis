# Composer Edit Menu

## Edit Menu Link Buttons

The page composer edit menu now presents its actions as links using shared button classes. The discard action keeps its danger-outlined cancel styling, while done and reset use confirm and neutral styling so the full menu is visually consistent.
