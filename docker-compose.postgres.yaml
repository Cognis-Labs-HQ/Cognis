services:
    db:
        image: postgres:17-alpine
        env_file:
            - docker/env/postgres.env
            - docker/env/production.env
            - docker/env/postgres-production.env
        environment:
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in docker/env/postgres-production.env}
        volumes:
            - pgdata:/var/lib/postgresql/data
        healthcheck:
            test:
                [
                    "CMD-SHELL",
                    "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
                ]
            interval: 10s
            timeout: 5s
            retries: 5

    cognis:
        build:
            context: .
            dockerfile: docker/Dockerfile
        image: cognis:prod
        env_file:
            - docker/env/default.env
            - docker/env/postgres.env
            - docker/env/production.env
            - docker/env/postgres-production.env
        environment:
            DATABASE_URL: postgresql://${POSTGRES_USER:?POSTGRES_USER must be set in docker/env/postgres.env}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in docker/env/postgres-production.env}@${POSTGRES_HOST:?POSTGRES_HOST must be set in docker/env/postgres.env}:${POSTGRES_PORT:?POSTGRES_PORT must be set in docker/env/postgres.env}/${POSTGRES_DB:?POSTGRES_DB must be set in docker/env/postgres.env}
            DATA_ENCRYPTION_KEY: ${DATA_ENCRYPTION_KEY:?DATA_ENCRYPTION_KEY must be set in docker/env/production.env}
        volumes:
            - applogs:/app/logs
            - appconfig:/app/config
            - appmedia:/app/media
        ports:
            - "3000:3000"
        depends_on:
            db:
                condition: service_healthy
        restart: unless-stopped

volumes:
    pgdata:
    applogs:
    appconfig:
    appmedia:
