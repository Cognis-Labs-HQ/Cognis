# UI Component

## Purpose
`ui/` will host the Cognis frontend and include a centralized documentation center consuming `/api/v1/docs`.

## Requirements
- Render docs index and per-component markdown from API.
- Keep layout modular to support pluggable module UI panels.
